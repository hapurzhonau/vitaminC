const absolute = (a) => {
  //finding absolute value of the number
  if (a >= 0) {
    return console.log(`(${a}) -- Number is not negative.`);
  } else a * -1 >= 0;
  {
    return console.log(`(${a}) --> (${a * -1}) -- Number became positive.`);
  }
};
absolute(-9); //enter any number in the breckets
